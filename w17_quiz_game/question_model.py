class Question:

    def __init__(self, text, answer):
        self.text = text
        self.answer = answer
    
    
    
#     print()
# new_question
# question = question_data[random.randint(0,len(question_data)-1)]["text"]
# answer = question_data[random.randint(0,len(question_data)-1)]["answer"]
# print(question["text"])d